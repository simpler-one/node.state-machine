{
  "name": "@working-sloth/state-machine",
  "version": "0.1.10",
  "description": "State Machine for NodeJS",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist & tsc -p src",
    "test": "npm run rm-spec & npm run build-spec & npm run ls-spec & jasmine",
    "rm-spec": "rimraf spec/built",
    "build-spec": "tsc -p spec",
    "ls-spec": "ls spec",
    "postinstall": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/work-work-komei/node.state-machine.git"
  },
  "keywords": [
    "fsm",
    "finite state machine",
    "state machine",
    "state-machine",
    "automaton",
    "state"
  ],
  "author": "Hiroaki Kobayashi",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/work-work-komei/node.state-machine/issues"
  },
  "homepage": "https://github.com/work-work-komei/node.state-machine#readme",
  "dependencies": {
    "rimraf": "^2.6.3",
    "tsc": "^1.20150623.0",
    "typescript": "^3.5.2"
  },
  "devDependencies": {
    "@types/jasmine": "^3.3.13",
    "jasmine": "^3.4.0"
  }
}
